source ./constants

test-err () {
    local exp="***** [.k0.err] [test_err]: foo"
    got="$(. .k0.err foo 2>&1)"
    # Quote because of the special chars in the string
    assertEquals "$exp" "$got"
}

. tryrun $*
