[ ! $0 == ${BASH_SOURCE[0]} ] && {
    echo ${BASH_SOURCE[0]} must be called not sourced
    return
}

pushd $(dirname $0) > /dev/null
here=$PWD
popd > /dev/null

main () {
    $1/flat/install
    . .k0.lbin $1/lbin
    k0 test
}
main $here
