. ./constants

test-resolve--link-one-level () {
    local real=$HOME
    local link1=$HOME/.tmp.$RANDOM
    ln -s $real $link1
    a=$(. .k0.resolve $link1)
    [ "$a" == "$HOME" ]
    rm $link1 
}

. tryrun $*
