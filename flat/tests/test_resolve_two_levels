. ./constants

test-resolve--link-two-levels () {
    local real=$HOME
    local link1=$HOME/.tmp.$RANDOM
    local link2=$HOME/.tmp.$RANDOM
    ln -s $real  $link1
    ln -s $link1 $link2
    a=$(. .k0.resolve $link2)
    [ "$a" == "$HOME" ]
    rm $link1 
}

. tryrun $*
