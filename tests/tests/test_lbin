. kt.init

test-lbin--works () {
    local src=$PWD/test.data/lbin.d
    local dst=$KT_SANDBOX/test_lbin/dst
    mkdir -p $dst
    export K0_BIN=$dst
    . .k0.lbin $src
    assertEquals "foo is here" "$(. foo)"
    assertEquals "bar is here" "$(. bar)"
    return 0
}

. kt.run $*
