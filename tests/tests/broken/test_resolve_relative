. kt.init

test-resolve--relative () {
    a=$(. .k0.resolve kt)
    # the space in the following expansion is crucial
    local tail="${a: -7}"
    echo 666 $a
    [ "$tail" == "kt.d/kt" ]
}

. kt.run $*
