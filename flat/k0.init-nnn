k0-init-nnn () {
    local here=$(K0-resolve $1)
    local name
    [ -f $here ] && here=$(dirname $here)
    for name in $(ls $here | sort | grep ^[0-9]); do
        local path=$here/$name
        k0-debug source $path
        source $path 
    done
}
k0-init-nnn "$@"
